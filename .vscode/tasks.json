{
    "tasks": [
        {
            "type": "shell",
            "label": "Make",
            "command": "/usr/bin/make",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build"
            }
        }
    ],
    "version": "2.0.0"
}